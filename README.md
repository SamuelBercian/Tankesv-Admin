# React + Vite

Antes de realizar cualquier cambio ejecutar lo siguiente:

-  npm install

Para ejecutar el proyecto utilizar el siguiente comando:

- npm run dev
- npm run build (Si no cargan diseños)
