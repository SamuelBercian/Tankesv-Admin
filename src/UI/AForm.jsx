export function AForm({label, href}){
    return(

        <section>
            <a className="
                xs:text-quaternary xs:text-sm
                2xs:text-quaternary 2xs:text-base
                
                " href="">You don't have an Account?</a>
        </section>

    );
};

export default AForm;